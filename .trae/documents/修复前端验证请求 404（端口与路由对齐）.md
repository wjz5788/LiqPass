## 现状确认
- 你的验证服务结果已生成：`apps/jp-verify/reports/evidence/2025-11-16/evi_20251116_115320_aefa64bb.json:1-32` 显示 `instId=BTC-USDT-SWAP`、`ordId=2940071038556348417`、`liq_flag=true`，说明 jp-verify 正常工作。
- 前端报错 `404` 来自 `http://localhost:3001/api/v1/verify/okx/standard`，并非 jp-verify 问题，而是前端请求发到了错误的端口（3001上没有这条路由）。

## 根因定位
- 前端调用相对路径：`apps/us-frontend/src/pages/ApiSettings.tsx:610` 使用 `fetch('/api/v1/verify/okx/standard')`；相对路径会落到“当前页面所在端口”。
- 你当前页面在 `3001`，所以请求去到了 `3001`（该端口没有 `/api/v1/verify/...`），导致 404。
- 正确的后端端口是 `3002`：`apps/us-backend/src/server.ts` 默认 `PORT=3002`，路由在 `apps/us-backend/src/routes/index.ts:30` 与 `okx-verify.ts:398-399`。

## 修复方案（二选一，可同时执行）
- 方案 A（代码层，推荐）：让所有 API 调用显式使用统一基址，而不是相对路径。
  - 在 `apps/us-frontend/src/pages/ApiSettings.tsx:609-617` 将 `fetch(path, ...)` 替换为 `api.post('/api/v1/verify/okx/standard', payload, { requireAuth: false })`。
  - `api` 的 `baseUrl` 来自 `ENV.VITE_API_BASE(URL)`（`apps/us-frontend/src/services/api.ts:151-156` + `apps/us-frontend/src/env.ts:52-55,87-94`），当前已指向 `http://127.0.0.1:3002`。
  - 效果：无论页面开在哪个端口（3000/3001/其他），都会命中 `3002` 的正确路由。
- 方案 B（环境层）：确保开发访问的是 Vite 端口 `3000` 或在 `3001` 配置反向代理。
  - 打开 `http://localhost:3000`（`apps/us-frontend/vite.config.ts:16-21` 会把 `/api` 代理到 `3002`）。
  - 如果必须在 `3001` 提供前端页面，为该服务配置 `/api` → `http://127.0.0.1:3002` 的反向代理（参考仓库部署文档的 Nginx 示例）。

## 验证流程
- 先查后端健康：`GET http://127.0.0.1:3002/api/v1/verify/health`（`okx-verify.ts:422-445`），返回 healthy。
- 前端页面执行“验证”，应返回 200 并展示标准视图；若遇 401 且处于开发模式，代码会自动降级直连 `jp-verify`（`apps/us-frontend/src/pages/ApiSettings.tsx:629-676`）。

## 结论
- jp-verify 已产出报告文件，证明验证链路正常；问题仅在前端请求目标为 `3001`。按上述方案对齐到 `3002` 即可恢复。